name: Validación HTML y CSS

on:
  push:
    branches: [ "main", "feature/*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Obtener código
        uses: actions/checkout@v4

      # Validación de HTML
      - name: Instalar y validar HTML con HTMLHint
        run: |
          npm install -g htmlhint
          htmlhint "**/*.html"

      # Validación de CSS
      - name: Instalar y validar CSS con Stylelint
        run: |
          npm install -g stylelint stylelint-config-standard
          stylelint "css/**/*.css"
